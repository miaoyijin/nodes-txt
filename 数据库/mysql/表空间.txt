1： 什么是表空间
innodb底层存储实现方式，mysiam 没有表空间概念

2： innodb存储数据有.FRM存放表定义，.ibd(独享表空间)，.ibdata(共享表空间).


3： 共享表空间存什么，mysql 系统表的信心，undo log ，rego log,double write buffer 等

4：独立表空间 保存独立的库数据，索引 等

5：inndodb有哪些表空间和ibdata又是什么关系
 innodb有如下表空间：ibtmp1(临时表空间)，undo tablespace(需要单独配置，如果没有配置则undolog还是保存到系统表空间),ibdata1(系统表空间), 独立表空间
 
 
 6： 表空间的逻辑构造
 
 表空间==》segment==> 区 extent  ==〉 页（是最小的I/O操作单位，也是实际上的物理概念在innodb中，是怎样定义一个页的？）  =》 ROW （最基本的数据保存单元）
segment段在这里也是一个逻辑的概念

在元数据表performance_schema下的表中，找不到段的信息

应该把重点 放到 区 和 页 里面

在innodb中，每一个索引都由两个段seg
