内存相关的技术mmap和mmu，sendfile

mmap函数和mmu的关系：mmap函数的基石是mmu，mmu内部又有mmap结构体

mmap零拷贝技术：减少了拷贝但是还是会发生系统调用，mmap是磁盘映射到内核，内核内存共享到用户空间，用户读写相当于操作内核空间内存，写入数据由操作系统输盘到文件，不需要用户操作。

内核台和用户态区分：指的是虚拟空间0-3g 是用户态，3g-4g 是内核态
