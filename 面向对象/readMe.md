POP 面向过程编程


OO 是面向对象

OOP 是面向对象编程

抽象类可以有抽象方法，也可以有实例方法；接口中所有的方法都是公开抽象方法。

2、子类使用extends关键字继承抽象类，子类使用implements关键字实现接口。子类实现接口必须重写接口中的所有方法。

3、抽象类可以有自己的构造方法，接口不能有构造方法。

4、一个具体类只能eqxtends一个抽象类(trait 实现多继承)，可以implements多个接口



面向对象关键字：
OO OOP OOD DDD AOP(一种oop思想，并不是一种设计模式，代理模式则是AOP的具体实现) GOF 等

DTO: